{
  "name": "mpeke-cy-library",
  "version": "1.0.0",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.11.2",
    "cypress": "^12.9.0",
    "gts": "^3.1.1",
    "typescript": "~4.7.0"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
